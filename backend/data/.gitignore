# Live runtime state — never commit
admin_sessions.json
*.tmp

# Allow this .gitignore itself + the .gitkeep marker so the directory
# is tracked even though all real contents are ignored
!.gitignore
!.gitkeep
